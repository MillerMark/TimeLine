<UserControl x:Class="TimeLineControl.TimeLine"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TimeLineControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<UserControl.Resources>
		<local:TimeLineEntryConverter x:Key="timeLineEntryConverter"/>
		<BooleanToVisibilityConverter x:Key="boolToVisibilityConverter" />
	</UserControl.Resources>
	<ListBox x:Name="lbTimeEntries" Background="{Binding Background, RelativeSource={RelativeSource Mode=FindAncestor, 
                                 AncestorType={x:Type UserControl}}}" MouseWheel="LbTimeEntries_MouseWheel">
		<ItemsControl.ItemTemplate>
			<DataTemplate DataType="{x:Type local:TimeLineEntry}">
				<Grid>
					<Rectangle Tag="{Binding Index}" Cursor="Hand" Height="24" Fill="#FF2C4B8A" VerticalAlignment="Stretch" HorizontalAlignment="Left" PreviewMouseDown="Rectangle_PreviewMouseDown" PreviewMouseMove="Rectangle_PreviewMouseMove" PreviewMouseUp="Rectangle_PreviewMouseUp">
						<Rectangle.ContextMenu>
							<ContextMenu>
								<MenuItem Header="Set Duration..." Click="MenuItem_Click"></MenuItem>
							</ContextMenu>
						</Rectangle.ContextMenu>
						<Rectangle.Margin>
							<MultiBinding Converter="{StaticResource timeLineEntryConverter}">
								<Binding Path="TotalDuration">
									<Binding.RelativeSource>
										<RelativeSource Mode="FindAncestor" AncestorType="{x:Type local:TimeLine}"/>
									</Binding.RelativeSource>
								</Binding>
								<Binding Path="Start"/>
								<Binding Path="ActualWidth">
									<Binding.RelativeSource>
										<RelativeSource Mode="FindAncestor" AncestorType="{x:Type local:TimeLine}"/>
									</Binding.RelativeSource>
								</Binding>
								<Binding Path="Start"/>
							</MultiBinding>
						</Rectangle.Margin>
						<Rectangle.Width>
							<MultiBinding Converter="{StaticResource timeLineEntryConverter}">
								<Binding Path="TotalDuration">
									<Binding.RelativeSource>
										<RelativeSource Mode="FindAncestor" AncestorType="{x:Type local:TimeLine}"/>
									</Binding.RelativeSource>
								</Binding>
								<Binding Path="Duration"/>
								<Binding Path="ActualWidth">
									<Binding.RelativeSource>
										<RelativeSource Mode="FindAncestor" AncestorType="{x:Type local:TimeLine}"/>
									</Binding.RelativeSource>
								</Binding>
								<Binding Path="Start"/>
							</MultiBinding>
						</Rectangle.Width>
					</Rectangle>
					<Rectangle Tag="{Binding Index}" Visibility="{Binding Path=DurationUnlocked, Converter={StaticResource boolToVisibilityConverter} }" Cursor="SizeWE" Width="6" Height="24" Fill="#FF778FBF" VerticalAlignment="Stretch" HorizontalAlignment="Left" PreviewMouseDown="ResizeRectangle_PreviewMouseDown" PreviewMouseMove="ResizeRectangle_PreviewMouseMove" PreviewMouseUp="Rectangle_PreviewMouseUp">
						<Rectangle.Margin>
							<MultiBinding Converter="{StaticResource timeLineEntryConverter}">
								<Binding Path="TotalDuration">
									<Binding.RelativeSource>
										<RelativeSource Mode="FindAncestor" AncestorType="{x:Type local:TimeLine}"/>
									</Binding.RelativeSource>
								</Binding>
								<Binding Path="End"/>
								<Binding Path="ActualWidth">
									<Binding.RelativeSource>
										<RelativeSource Mode="FindAncestor" AncestorType="{x:Type local:TimeLine}"/>
									</Binding.RelativeSource>
								</Binding>
								<Binding Path="Start"/>
							</MultiBinding>
						</Rectangle.Margin>
					</Rectangle>
					<TextBlock Text="{Binding Name}" Height="Auto" Foreground="White" Padding="3,3,3,3" IsHitTestVisible="False">
						<TextBlock.Margin>
							<MultiBinding Converter="{StaticResource timeLineEntryConverter}">
								<Binding Path="TotalDuration">
									<Binding.RelativeSource>
										<RelativeSource Mode="FindAncestor" AncestorType="{x:Type local:TimeLine}"/>
									</Binding.RelativeSource>
								</Binding>
								<Binding Path="Start"/>
								<Binding Path="ActualWidth">
									<Binding.RelativeSource>
										<RelativeSource Mode="FindAncestor" AncestorType="{x:Type local:TimeLine}"/>
									</Binding.RelativeSource>
								</Binding>
								<Binding Path="Start"/>
							</MultiBinding>
						</TextBlock.Margin>
						<TextBlock.Width>
							<MultiBinding Converter="{StaticResource timeLineEntryConverter}">
								<Binding Path="TotalDuration">
									<Binding.RelativeSource>
										<RelativeSource Mode="FindAncestor" AncestorType="{x:Type local:TimeLine}"/>
									</Binding.RelativeSource>
								</Binding>
								<Binding Path="Duration"/>
								<Binding Path="ActualWidth">
									<Binding.RelativeSource>
										<RelativeSource Mode="FindAncestor" AncestorType="{x:Type local:TimeLine}"/>
									</Binding.RelativeSource>
								</Binding>
								<Binding Path="Start"/>
							</MultiBinding>
						</TextBlock.Width>
					</TextBlock>
				</Grid>
			</DataTemplate>

		</ItemsControl.ItemTemplate>
	</ListBox>
</UserControl>
